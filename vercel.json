{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build && mkdir -p dist && (cp -r dist/public/* dist/ 2>/dev/null || true) && ls -la dist/index.html dist/assets/ 2>/dev/null || echo 'Build files ready'",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}