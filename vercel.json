{
  "buildCommand": "npm install && npm run build:sitemap && cd client && SKIP_ENV_VALIDATION=true npx vite build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && cd client && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
